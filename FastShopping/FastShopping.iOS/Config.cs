using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Foundation;
//using SQLite.Net.Interop;
using UIKit;

namespace FastShopping.iOS
{
    public class Config
    {
        private readonly string dbDir;
        /*private readonly ISQLitePlatform platform;

        public Config()
        {
            dbDir = System.Environment.GetFolderPath(System.Environment.SpecialFolder.ApplicationData);
            platform = new SQLite.Net.Platform.XamarinIOS.SQLitePlatformIOS();

        }

        public string DbDir => dbDir;

        public ISQLitePlatform Platform => platform;*/
    }
}